<h1><%= t(:editing_report) %></h1>

<p>
  <strong><%= Report.human_attribute_name(:created_at) %>:</strong>
  <%= l(@report.created_at.to_date, format: :long) %>
</p>

<%= render 'form', report: @report %>

<%= link_to t(:show), @report %> |
<%= link_to t(:back), reports_path %>
